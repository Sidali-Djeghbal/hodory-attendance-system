import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    padding: 16,
    width: "100%",
    height: "100%",
    alignItems: "center",
    justifyContent: "center",
    gap: 16,
  },
  image: {
    marginTop: 60,
    width: 300,
    height: 300,
  },
});
